[tox]
envlist = py, lint

[testenv]
deps = -r requirements.txt
commands = pytest
setenv =
    PYTHONPATH = {toxinidir}

[testenv:lint]
deps = flake8
commands = flake8 app tests

[flake8]
max-line-length = 100
per-file-ignores =
    tests/*: E501,W391,W292,E303,F401